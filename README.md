# Getting started

```bash
git clone --recurse-submodules git@github.com:lucaayscough/automate

mkdir build && cmake -B build -DCMAKE_BUILD_TYPE=Debug -DPLUGIN_TYPE=Effect && cmake --build build

# You can find the built binaries in e.g.
# build/Automate-FX_artefacts/Debug/Standalone
```
